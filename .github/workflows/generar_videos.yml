name: Generador de Videos Virales

on:
  workflow_dispatch:
    inputs:
      videos:
        description: 'Número de videos por día'
        required: true
        default: '5'
        type: string
      tema:
        description: 'Tema específico o aleatorio (escribe aleatorio o todo)'
        required: true
        default: 'aleatorio'
        type: string
  schedule:
    - cron: '0 8 * * *'

jobs:
  construir-videos:
    runs-on: ubuntu-latest

    steps:
      - name: Clonar repositorio
        uses: actions/checkout@v3

      - name: Configurar Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Instalar dependencias del sistema y fuentes
        run: |
          sudo apt-get update
          sudo apt-get install -y fonts-dejavu

      - name: Instalar librerías de Python
        run: |
          python -m pip install --upgrade pip
          pip install moviepy requests pillow

      - name: Ejecutar generador de videos
        env:
          PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
        run: python por_dia.py --videos ${{ github.event.inputs.videos || 5 }} --tema ${{ github.event.inputs.tema || 'aleatorio' }}
